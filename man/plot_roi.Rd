% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/controllers_plot_controller.R,
%   R/generics_plot.R
\name{plot_roi}
\alias{plot_roi}
\alias{plot_roi.data.frame}
\alias{plot_roi.genome_entity}
\alias{plot_roi.default}
\title{Plot a static genomic region of interest}
\usage{
plot_roi(roi, ...)

\method{plot_roi}{data.frame}(
  roi,
  title = NULL,
  arrow_height = 0.18,
  head_prop = 0.35,
  neck_prop = 0.6,
  label_size = 4,
  colors = NULL,
  ...
)

\method{plot_roi}{genome_entity}(
  roi,
  gene = NULL,
  contig = NULL,
  start = NULL,
  end = NULL,
  flank = NULL,
  feature_type = "CDS",
  title = NULL,
  arrow_height = 0.18,
  head_prop = 0.35,
  neck_prop = 0.6,
  label_size = 4,
  colors = NULL,
  ...
)

\method{plot_roi}{default}(roi, ...)
}
\arguments{
\item{roi}{A \code{genome_entity} (or \code{genome_entity_gd}) object.}

\item{...}{Passed to the class-specific method.}

\item{title}{\code{character(1)} Plot title. Default \code{NULL} produces an
auto-generated title describing the plotted region.}

\item{arrow_height}{\code{numeric(1)} Total vertical extent of each arrow in
internal y-units. Increase for taller, more prominent arrows. Default
\code{0.18}.}

\item{head_prop}{\code{numeric(1)} Fraction of each gene's length occupied by
the arrowhead, in the range (0, 1]. Default \code{0.35}.}

\item{neck_prop}{\code{numeric(1)} Height of the arrow body as a fraction of
the full arrowhead height, in the range (0, 1]. \code{1} gives a rectangular
body; values below \code{1} create a narrowed neck. Default \code{0.60}.}

\item{label_size}{\code{numeric(1)} Text size for gene labels as accepted by
\code{\link[ggplot2:geom_text]{ggplot2::geom_text()}}. Default \code{4.0}.}

\item{colors}{\code{character} or \code{NULL}. When \code{NULL} (default) colours are
drawn from an evenly-spaced HCL palette matching ggplot2 defaults. Supply
a named vector to map gene names to specific colours, or an unnamed
vector that is recycled across genes in plotting order.}

\item{gene}{\code{character(1)} Gene name (or pattern) to look up. Mutually
exclusive with \code{contig}/\code{start}/\code{end}. The search is case-insensitive
and matches \code{ID}, \code{Name}, \code{Alias}, \code{gene}, \code{locus_tag}, and \code{product}
fields. Features of type \code{"gene"} are excluded from matching to avoid
duplicate hits common in NCBI-style annotations. If exactly one feature
matches, its coordinates plus \code{flank} define the plotted window.}

\item{contig}{\code{character(1)} Sequence name. Ignored when \code{gene} is supplied
(the contig is inferred from the matched feature). In coordinate mode,
when omitted the first contig returned by \code{\link[=get_contig_names]{get_contig_names()}} is used
with an informational message.}

\item{start, end}{\code{integer(1)} Region boundaries in coordinate mode. Both
must be supplied together. Ignored when \code{gene} is supplied.}

\item{flank}{\code{integer(1)} Base pairs added on each side of the target
region. Defaults to \code{5000} in gene mode and \code{0} in coordinate mode.
Override explicitly to zoom in or out.}

\item{feature_type}{\code{character} Feature type(s) passed to
\code{\link[=get_roi_features]{get_roi_features()}} and drawn in the plot. Default \code{"CDS"}.}
}
\value{
A \code{\link[ggplot2]{ggplot}} object. Further layers and theme
adjustments can be added with \code{+} in the usual way.
}
\description{
Draws a linear map of a genomic region. Dispatches on the class of the
first argument:
\itemize{
\item \strong{\code{data.frame}} (or \code{GRanges} / any coercible
object): draws directly from a pre-built feature table, typically
produced by \code{\link{get_roi_features}()}.
\item \strong{\code{genome_entity}}: looks up a gene by name \emph{or}
accepts explicit coordinates, fetches the feature table automatically,
then draws the result.
}

\strong{data.frame method:} Draws a linear map of a genomic region returned
by \code{\link[=get_roi_features]{get_roi_features()}}.
Each feature is rendered as a directional arrow coloured uniquely per gene.
Gene labels are placed above the track with automatic 2-D collision
avoidance: overlapping labels are first nudged horizontally (within the
plot bounds) and, when horizontal room is exhausted, lifted to a higher
tier. Leader lines connect displaced labels back to their arrows.

Requires the \strong{ggplot2} package. Install it with
\code{install.packages("ggplot2")} if prompted.
}
\examples{
roi <- data.frame(
  seqnames = "U00096",
  start    = c(1971030, 1972836, 1973360, 1975329, 1976252,
               1977266, 1977847, 1978518, 1979753, 1980188, 1981587),
  end      = c(1972691, 1973339, 1975324, 1976255, 1977139,
               1977844, 1978197, 1978966, 1980181, 1981612, 1982387),
  strand   = c("-","-","-","-","-","-","-","-","+","-","-"),
  type     = "CDS",
  Name     = c("tar","cheW","cheA","motB","motA",
               "flhC","flhD","pgaptmp_001971","uspC","otsA","otsB"),
  stringsAsFactors = FALSE
)
if (requireNamespace("ggplot2", quietly = TRUE)) {
  plot_roi(roi)
}
}
\seealso{
\code{\link[=get_roi_features]{get_roi_features()}} to obtain a region of interest from a genome
object. \code{\link[=plot_cgview]{plot_cgview()}} for an interactive circular genome map.
}
