% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/queries.R
\name{get_roi_fasta_batch}
\alias{get_roi_fasta_batch}
\title{Extract Multiple ROI Sequences as Batch}
\usage{
get_roi_fasta_batch(genome_obj, roi_table, flank = 0, output_file = NULL)
}
\arguments{
\item{genome_obj}{Genome object from init_genome()}

\item{roi_table}{Data frame with columns: seqname, start, end, and optionally strand, name}

\item{flank}{Flanking region size in bp (default = 0)}

\item{output_file}{Optional file path to write all sequences (default = NULL)}
}
\value{
DNAStringSet with all extracted sequences
}
\description{
Extract Multiple ROI Sequences as Batch
}
\examples{
\dontrun{
# Define multiple regions
regions <- tibble(
  seqname = c("1", "1", "1"),
  start = c(100000, 200000, 300000),
  end = c(150000, 250000, 350000),
  name = c("region1", "region2", "region3")
)

# Extract all at once
seqs <- get_roi_fasta_batch(genome, regions, flank = 1000)

# Save to file
get_roi_fasta_batch(genome, regions, 
                    flank = 1000, 
                    output_file = "all_regions.fasta")
}
}
