% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/entities_validators.R
\name{validate_genomic_coordinates}
\alias{validate_genomic_coordinates}
\title{Validate Genomic Coordinates}
\usage{
validate_genomic_coordinates(start, end, seqname = NULL, max_length = NULL)
}
\arguments{
\item{start}{Integer start position (1-based)}

\item{end}{Integer end position (1-based, inclusive)}

\item{seqname}{Character sequence name (for error messages)}

\item{max_length}{Integer maximum sequence length (optional)}
}
\value{
TRUE if valid, stops with error if invalid
}
\description{
Validates that genomic coordinates satisfy domain rules:
\itemize{
\item Start position must be positive (1-based)
\item Start must be <= end
\item Coordinates must be within sequence bounds (if max_length provided)
}
}
\examples{
# Valid coordinates
validate_genomic_coordinates(1, 100, "chr1")
validate_genomic_coordinates(50, 150, "chr1", max_length = 200)

\dontrun{
# Invalid: negative start
validate_genomic_coordinates(-1, 100, "chr1")  # Error

# Invalid: start > end
validate_genomic_coordinates(100, 50, "chr1")  # Error

# Invalid: exceeds bounds
validate_genomic_coordinates(1, 200, "chr1", max_length = 100)  # Error
}
}
