% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/controllers_query_controller.R
\name{extract_sequences_by_coords}
\alias{extract_sequences_by_coords}
\title{Extract Sequences by Genomic Coordinates}
\usage{
extract_sequences_by_coords(x, ...)
}
\arguments{
\item{x}{A genome_entity object}

\item{...}{Additional arguments (currently unused)}

\item{seqname}{Character; sequence name(s)}

\item{start}{Integer; start position(s) (1-based, inclusive)}

\item{end}{Integer; end position(s) (1-based, inclusive)}

\item{strand}{Character; strand ("+", "-", or NULL for forward strand)}

\item{translate}{Logical; translate to protein (default FALSE)}

\item{names}{Character vector of names for output sequences (optional)}
}
\value{
Character vector of sequences (named if names provided)
}
\description{
Extract DNA sequences from specified genomic coordinates. Supports extracting
regions from one or more sequences with optional reverse complement.

This is a controller function that calls the extract_sequences_by_coords use case.
}
\examples{
\dontrun{
genome <- read_genome("data.gbk")

# Extract single region
seq <- extract_sequences_by_coords(genome, "chr1", 1, 100)

# Extract multiple regions
seqs <- extract_sequences_by_coords(
  genome,
  seqname = c("chr1", "chr1", "plasmid"),
  start = c(1, 500, 1),
  end = c(100, 600, 200)
)

# Extract with reverse complement
seq <- extract_sequences_by_coords(
  genome,
  "chr1", 1, 100,
  strand = "-"
)
}
}
