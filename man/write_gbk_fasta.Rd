% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/controllers_export_controller.R,
%   R/genbank_export.R
\name{write_gbk_fasta}
\alias{write_gbk_fasta}
\title{Write GenBank and FASTA (Combined Export)}
\usage{
write_gbk_fasta(
  x,
  file,
  wrap_width = 80L,
  auto_label = TRUE,
  include_length = TRUE
)

write_gbk_fasta(
  x,
  file,
  wrap_width = 80L,
  auto_label = TRUE,
  include_length = TRUE
)
}
\arguments{
\item{x}{A genome_entity object or list of GenBank records}

\item{file}{Output FASTA file path}

\item{wrap_width}{Integer; wrap sequences at this width (default 80)}

\item{auto_label}{Logical; if TRUE (default), infer chromosome vs plasmids}

\item{include_length}{Logical; append length annotation to header (default TRUE)}

\item{...}{Additional arguments passed to write_fasta}
}
\value{
Invisibly returns the file path

Invisibly returns output path
}
\description{
Export genome to FASTA format. This is a convenience wrapper for write_fasta()
that maintains backward compatibility with the old API.

Writes genome sequences to FASTA format. Accepts both genome_entity objects
and legacy GenBank list format for backward compatibility.
}
\examples{
\dontrun{
genome <- read_genome("data.gbk")
write_gbk_fasta(genome, "output.fasta")
}
}
