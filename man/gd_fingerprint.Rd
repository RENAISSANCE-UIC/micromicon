% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gd_helpers.R
\name{gd_fingerprint}
\alias{gd_fingerprint}
\title{Fingerprint helper (base-R, no digest)}
\usage{
gd_fingerprint(x, file = FALSE, algo = "md5")
}
\arguments{
\item{x}{File path (if file = TRUE) or an arbitrary R object (if file = FALSE).}

\item{file}{Logical. If TRUE, treat \code{x} as a file path.}

\item{algo}{Character. Ignored unless \code{"md5"}. Provided for API compatibility.}
}
\value{
A length-1 character vector with the md5 fingerprint.
}
\description{
Compute an md5 fingerprint for a file or an R object without importing \code{digest}.
For files we call tools::md5sum(). For in-memory objects we serialize to a
temp file (stable RDS) then md5 that file. This is intentionally simple,
reproducible, and dependency-free.
}
\examples{
# files
# gd_fingerprint("annotated.gd", file = TRUE)

# objects
# gd_fingerprint(list(a = 1, b = letters[1:3]))
}
