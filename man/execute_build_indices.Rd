% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_cases_transform_build_indices.R
\name{execute_build_indices}
\alias{execute_build_indices}
\title{Build Indices for Fast Lookup}
\usage{
execute_build_indices(features_df, metadata_df)
}
\arguments{
\item{features_df}{data.frame with features (must have seqname column)}

\item{metadata_df}{data.frame with sequence metadata (must have seqname column)}
}
\value{
List with indices components
}
\description{
Use case that builds indices for fast feature lookup. This is pure business logic
that operates on data frames without any I/O or framework dependencies.

Indices built:
\itemize{
\item seqnames: Unique sequence names
\item locus_tag_index: Map locus_tag to feature row number
\item gene_index: Map gene name to feature row number(s)
}
}
\examples{
features <- data.frame(
  seqname = c("chr1", "chr1", "chr2"),
  locus_tag = c("GENE001", "GENE002", "GENE003"),
  gene = c("geneA", "geneB", "geneC"),
  stringsAsFactors = FALSE
)

metadata <- data.frame(
  seqname = c("chr1", "chr2"),
  length = c(1000, 500),
  stringsAsFactors = FALSE
)

indices <- execute_build_indices(features, metadata)
}
