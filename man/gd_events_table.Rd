% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{gd_events_table}
\alias{gd_events_table}
\title{Convert genome_entity_gd events to a data frame}
\usage{
gd_events_table(
  gd,
  expand_tags = TRUE,
  include_raw = FALSE,
  include_hash = TRUE,
  types = NULL,
  kinds = NULL,
  n = Inf,
  cols = NULL,
  stringsAsFactors = FALSE
)
}
\arguments{
\item{gd}{A genome_entity_gd object}

\item{expand_tags}{Logical; if TRUE, flatten tags into separate tag_* columns;
if FALSE, keep as a list-column}

\item{include_raw}{Logical; include the raw_line field}

\item{include_hash}{Logical; include the event hash field}

\item{types}{Character vector of event types to filter (e.g., c("SNP", "DEL"))}

\item{kinds}{Character vector of event kinds to filter: "mutation", "evidence", "validation"}

\item{n}{Maximum number of rows to return (default Inf for all)}

\item{cols}{Character vector of column names to prioritize in output order}

\item{stringsAsFactors}{Logical; passed to as.data.frame()}
}
\value{
A data frame (or tibble if available) with one row per event
}
\description{
Converts the nested event list from a genome_entity_gd object into a tidy
data frame with one row per event. Tags can either be expanded into individual
columns (tag_*) or kept as a list-column for programmatic access.
}
\examples{
\dontrun{
  # Get all events with tags as list-column
  ev_tbl <- gd_events_table(gd, expand_tags = FALSE)

  # Get only mutations with expanded tags
  mut_tbl <- gd_events_table(gd, kinds = "mutation", expand_tags = TRUE)

  # Filter to specific types
  snps <- gd_events_table(gd, types = c("SNP", "DEL"), n = 100)
}
}
