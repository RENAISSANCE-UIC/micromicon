% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blast_local.R, R/queries.R
\name{reduce_hits}
\alias{reduce_hits}
\title{Reduce BLAST hits with cov/identity thresholds; select best hit or top-N per query}
\usage{
reduce_hits(
  hits,
  min_qcov = 40,
  min_pident = 25,
  besthit = TRUE,
  max_per_query = NULL
)

reduce_hits(
  hits,
  min_qcov = 40,
  min_pident = 25,
  besthit = TRUE,
  max_per_query = NULL
)
}
\arguments{
\item{hits}{Data frame of BLAST hits (from blastp_capture or blastp_roi)}

\item{min_qcov}{Minimum query coverage percentage (default 40)}

\item{min_pident}{Minimum percent identity (default 25)}

\item{besthit}{Logical; if TRUE, return only best hit per query (default TRUE)}

\item{max_per_query}{If not NULL and besthit=FALSE, return top N hits per query}
}
\value{
Filtered tibble of BLAST hits
}
\description{
Reduce BLAST hits with cov/identity thresholds; select best hit or top-N per query

Reduce BLAST hits with cov/identity thresholds; select best hit or top-N per query
}
