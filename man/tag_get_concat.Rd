% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/controllers_gd_views.R
\name{tag_get_concat}
\alias{tag_get_concat}
\title{Concatenate all values of a tag into a column}
\usage{
tag_get_concat(tbl, name, into = paste0("tag_", name), sep = "|")
}
\arguments{
\item{tbl}{A data frame from \code{gd_events_table()} with a tags list-column}

\item{name}{Name of the tag to extract (without "tag_" prefix)}

\item{into}{Name for the new column (default: "tag_{name}")}

\item{sep}{Separator for concatenating multiple values (default "|")}
}
\value{
The input data frame with the new column added
}
\description{
Extracts all values of a multi-valued tag from the tags list-column,
concatenates them with a separator, and creates a new column.
}
\examples{
\dontrun{
  ev_tbl <- gd_events_table(gd, expand_tags = FALSE)
  ev_tbl <- tag_get_concat(ev_tbl, "locus_tag", sep = ";")
  ev_tbl$tag_locus_tag  # New column with concatenated values
}
}
\seealso{
\code{\link{tag_get_first}}, \code{\link{gd_events_table}}
}
\keyword{internal}
