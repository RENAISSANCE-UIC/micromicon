% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/controllers_accessor_controller.R,
%   R/generics_accessors.R
\name{features}
\alias{features}
\title{Access Features from genome_entity}
\usage{
features(x, ...)

features(x, ...)
}
\arguments{
\item{x}{Object to extract features from}

\item{...}{Additional arguments}

\item{format}{Character; output format:
\itemize{
\item "data.frame": data.frame with features (default)
\item "GRanges": GenomicRanges GRanges object (requires GenomicRanges)
}}

\item{type}{Character; filter by feature type (e.g., "gene", "CDS")}
}
\value{
Features in requested format
}
\description{
Extract features from a genome_entity object as data.frame or GRanges.
}
\examples{
\dontrun{
genome <- read_genome("data.gbk")

# All features as data.frame
feats <- features(genome)

# Only genes
genes <- features(genome, type = "gene")

# As GRanges (if GenomicRanges available)
gr <- features(genome, format = "GRanges")
}
}
