% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{tag_get_first}
\alias{tag_get_first}
\title{Extract first value of a tag into a column}
\usage{
tag_get_first(tbl, name, into = paste0("tag_", name))
}
\arguments{
\item{tbl}{A data frame from \code{gd_events_table()} with a tags list-column}

\item{name}{Name of the tag to extract (without "tag_" prefix)}

\item{into}{Name for the new column (default: "tag_{name}")}
}
\value{
The input data frame with the new column added
}
\description{
Extracts the first value of a multi-valued tag from the tags list-column
and creates a new scalar column. Returns NA if the tag is absent.
}
\examples{
\dontrun{
  ev_tbl <- gd_events_table(gd, expand_tags = FALSE)
  ev_tbl <- tag_get_first(ev_tbl, "gene_name")
  ev_tbl$tag_gene_name  # New column with first gene_name value
}
}
\seealso{
\code{\link{tag_get_concat}}, \code{\link{gd_events_table}}
}
