% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blast_local.R, R/queries.R
\name{blastp_roi}
\alias{blastp_roi}
\title{Run BLASTP on one or more query FAA files and return tidy hits}
\usage{
blastp_roi(
  faa_path,
  db = "swissprot",
  dbdir = Sys.getenv("BLASTDB", ""),
  blastp_bin = Sys.getenv("BLASTP_BIN", ""),
  blastp_dir = Sys.getenv("BLASTP_PATH", ""),
  evalue = 1e-10,
  threads = parallel::detectCores(),
  fields = c("qseqid", "sacc", "stitle", "pident", "length", "qcovs", "bitscore",
    "evalue", "staxids"),
  validate_db = TRUE,
  more_args = character()
)

blastp_roi(
  faa_path,
  db = "swissprot",
  dbdir = Sys.getenv("BLASTDB", ""),
  blastp_bin = Sys.getenv("BLASTP_BIN", ""),
  blastp_dir = Sys.getenv("BLASTP_PATH", ""),
  evalue = 1e-10,
  threads = parallel::detectCores(),
  fields = c("qseqid", "sacc", "stitle", "pident", "length", "qcovs", "bitscore",
    "evalue", "staxids"),
  validate_db = TRUE,
  more_args = character()
)
}
\arguments{
\item{faa_path}{Character vector of one or more query FASTA/FAA file paths}

\item{db}{BLAST database prefix (e.g., "swissprot" or absolute "/path/to/swissprot")}

\item{dbdir}{Directory containing BLAST DBs; if db is relative, dbdir is prepended}

\item{blastp_bin}{Full path to blastp binary (preferred explicit control)}

\item{blastp_dir}{Directory containing blastp; used if blastp_bin is NULL}

\item{evalue}{E-value threshold}

\item{threads}{Integer number of threads}

\item{fields}{Vector of outfmt 6 columns}

\item{validate_db}{Logical; check DB via blastdbcmd before running}

\item{more_args}{Character vector of additional BLASTP args (e.g., c("-max_target_seqs","10"))}
}
\value{
tibble of combined BLAST hits from all queries
}
\description{
Inherits binary/db controls from blastp_capture; no reticulate involved.
}
